import { ScrollView, Text } from 'react-native'

const Agreement = () => {
    return (
        <ScrollView style={{ flex: 1, paddingInline: 25 }}>
            <Text> textInComponent </Text>
        </ScrollView>
    )

}

export default Agreement